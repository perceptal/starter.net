@{ Html.RenderPartial("Users/Menu"); }

@{ Html.RenderPartial("User"); }

<div class="content">
    @{ Html.RenderPartial("Users/Actions"); }

    <section>
        <fieldset>
            <label>First Name</label>
            <input type="text" />

            <label>Last Name</label>
            <input type="text" />

            <input type="submit" value="Save&rarr;" />
        </fieldset>
    </section>
</div>
