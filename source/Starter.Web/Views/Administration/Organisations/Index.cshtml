@model OrganisationListViewModel

@{ Html.RenderPartial("Administration/Organisations/Menu"); }

@foreach (var org in Model.Organisations) 
{
    Html.RenderPartialWithData("Organisation", org, new { @class = "list" });
}
    