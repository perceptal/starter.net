@model MemberListViewModel

@{ Html.RenderPartial("Members/Menu"); }

@foreach (var member in Model.Members) 
{
    Html.RenderPartialWithData("Member", member, new { @class = "list" });
}
    